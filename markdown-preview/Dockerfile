# Step 1: Use the official Nginx image as the base image
FROM nginx:alpine

# Step 2: Copy your static files (HTML, CSS, JS) to the Nginx html directory
COPY ./ /usr/share/nginx/html

# Step 3: Expose port 80 to serve the app
EXPOSE 80
